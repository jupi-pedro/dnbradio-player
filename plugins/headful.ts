// vue-headful is replaced with useHead in Nuxt 3
// This plugin is kept for backward compatibility but does nothing
export default defineNuxtPlugin(() => {
  // No-op: use useHead() composable instead
})
